name: Deploy — Production

on:
  push:
    branches: [main]

jobs:

  # ── Deploy via EasyPanel API (box service — git clone + build.sh) ─────────────
  deploy:
    name: Deploy to EasyPanel
    runs-on: ubuntu-latest
    environment:
      name: production
      url: https://prowaylab.com

    steps:
      - name: Trigger EasyPanel rebuild
        env:
          EASYPANEL_PASSWORD: ${{ secrets.EASYPANEL_PASSWORD }}
        run: |
          if [ -z "$EASYPANEL_PASSWORD" ]; then
            echo "EASYPANEL_PASSWORD not configured — redeploy manually from EasyPanel."
            exit 0
          fi
          # Step 1: Login to get JWT token
          TOKEN=$(curl -s -X POST "https://panel.wellcorefitness.com/api/trpc/auth.login" \
            -H "Content-Type: application/json" \
            -d "{\"json\":{\"email\":\"info@wellcorefitness.com\",\"password\":\"$EASYPANEL_PASSWORD\"}}" \
            | python3 -c "import sys,json; print(json.load(sys.stdin)['result']['data']['json']['token'])")
          if [ -z "$TOKEN" ]; then
            echo "Failed to obtain EasyPanel token — check EASYPANEL_PASSWORD secret."
            exit 1
          fi
          echo "EasyPanel login successful."
          # Step 2: Trigger rebuild with token
          RESPONSE=$(curl -s -o /tmp/ep_response.txt -w "%{http_code}" \
            -X POST "https://panel.wellcorefitness.com/api/trpc/services.box.rebuildDockerImage" \
            -H "Authorization: Bearer $TOKEN" \
            -H "Content-Type: application/json" \
            -d '{"json":{"projectName":"wellcorefitness","serviceName":"prowaylab"}}')
          echo "EasyPanel response: $RESPONSE"
          cat /tmp/ep_response.txt || true
          if [ "$RESPONSE" = "200" ]; then
            echo "EasyPanel rebuild triggered successfully."
          else
            echo "EasyPanel returned $RESPONSE — rebuild failed."
            exit 1
          fi

      - name: Restart service to clear OPcache
        env:
          EASYPANEL_PASSWORD: ${{ secrets.EASYPANEL_PASSWORD }}
        run: |
          if [ -z "$EASYPANEL_PASSWORD" ]; then exit 0; fi
          TOKEN=$(curl -s -X POST "https://panel.wellcorefitness.com/api/trpc/auth.login" \
            -H "Content-Type: application/json" \
            -d "{\"json\":{\"email\":\"info@wellcorefitness.com\",\"password\":\"$EASYPANEL_PASSWORD\"}}" \
            | python3 -c "import sys,json; print(json.load(sys.stdin)['result']['data']['json']['token'])")
          # Deploy the service (git pull + restart)
          curl -s -X POST "https://panel.wellcorefitness.com/api/trpc/services.box.deploy" \
            -H "Authorization: Bearer $TOKEN" \
            -H "Content-Type: application/json" \
            -d '{"json":{"projectName":"wellcorefitness","serviceName":"prowaylab"}}' || true
          echo "Deploy + restart triggered."

      - name: Wait for build + health check
        run: |
          echo "Waiting 3min for EasyPanel build + deploy..."
          sleep 180
          for i in 1 2 3; do
            BODY=$(curl -s https://prowaylab.com/api/health.php)
            HTTP=$(echo "$BODY" | python3 -c "import sys,json; d=json.load(sys.stdin); print(d.get('deploy_id','?'))" 2>/dev/null || echo "?")
            echo "Attempt $i: deploy_id=$HTTP"
            if echo "$BODY" | grep -q '"status":"ok"'; then
              echo "Health check passed! deploy_id=$HTTP"
              exit 0
            fi
            sleep 20
          done
          echo "Health check could not confirm deployment (may still be building)"
          exit 0
